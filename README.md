# todo-fullstack
A full-stack Todo application that allows users to create, view, update, and delete tasks. The application uses a React frontend that communicates with a Node.js backend through REST APIs. The backend handles the business logic, API requests, and data management.
